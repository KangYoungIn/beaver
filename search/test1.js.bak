const botkit = require('botkit');
const slack = require('slack-node');

function convert(input) {
    let query = extract(input);
    return "https://www.google.co.kr/serach?q=" + query;
}

function extract(input) {
    return input.split("검색!")[1]
}

const controller = botkit.slackbot({
    debug:false,
    log:true
});

const botScope = [
    'direct_message',
    'direct_mention',
    'mention'
];

const convertmessage = convert(kangyoungin)

controller.hears(['test'], botScope, (bot, message) => {
    bot.reply(message, convertmessage);
});


controller.spawn({
    token:'xoxb-661033479312-661839074306-QZ6jZeWifUxP9601wymXulXy'
}).startRTM();

